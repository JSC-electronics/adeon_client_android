// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    // Define versions in a single place
    ext {
        // Sdk and tools
        compileSdkVersion = 29
        minSdkVersion = 19
        targetSdkVersion = 29

        // App dependencies
        appIntroVersion = '6.0.0-alpha3'
        billingVersion = '2.1.0'
        circleImageViewVersion = '3.0.0'
        constraintLayoutVersion = '2.0.0-beta4'
        coroutinesVersion = "1.1.1"
        espressoVersion = '3.2.0-beta01'
        glideVersion = '4.9.0'
        gradleVersion = '3.6.0'
        grgitVersion = '3.1.1'
        gsonVersion = '2.8.5'
        junitVersion = '4.12'
        kotlinVersion = '1.3.61'
        ktlintVersion = '0.30.0'
        ktxVersion = '1.3.0-alpha01'
        lifecycleVersion = '2.2.0'
        materialVersion = '1.1.0'
        multiDexLibraryVersion = '2.0.1'
        navigationVersion = '2.3.0-alpha02'
        ossLicensesLibrary = '17.0.0'
        ossLicensesPlugin = '0.10.0'
        phoneFieldVersion = '0.1.7'
        playServicesAddsVersion = '19.0.0'
        recyclerViewVersion = '1.2.0-alpha01'
        roomVersion = '2.2.4'
        supportLibraryVersion = '1.1.0'
    }

    repositories {
        google()
        jcenter()
        maven { url 'http://dl.bintray.com/kotlin/kotlin-eap' }
    }
    dependencies {
        classpath "com.android.tools.build:gradle:$gradleVersion"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
        classpath "androidx.navigation:navigation-safe-args-gradle-plugin:$navigationVersion"
        classpath "com.google.android.gms:oss-licenses-plugin:$ossLicensesPlugin"
        classpath "org.ajoberstar.grgit:grgit-gradle:$grgitVersion"
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url 'https://dl.bintray.com/jsc-electronics/maven' }
        maven { url 'https://jitpack.io' }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
